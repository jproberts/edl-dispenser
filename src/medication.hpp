#include <string>

class Medication
{
  private:
    string Name;
    int Frequency;
    int TimeOfDay;
    string DoctorContactInfo;
    int Supply;
    enum Notifications;
    int UniqueId;
    // User* Recipients // Hoping we don't need this.
    int ContainerNum;

  public:
}